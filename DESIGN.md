# DESIGN.md — Guides-tours

> Hard contract. Authority: this file > `design-arsenal` > `AGENTS.md`.

## Direction

**Field operator / Mediterranean travel** — sand, sea, coral. Photography leads.
Instrument Serif for display. The operator console is a week board, not a generic
admin. Landing and app must share tokens.

### References
- Manshausen / Menu Space — place, not SaaS
- Linear — operator density
- Page Flows — booking / assign sequences

### NEVER
- Stock zinc/slate oklch as the product look
- Inter as `--font-sans`
- Equal FeatureCard trios + `muted-foreground` soup
- Four equal StatCards as the dashboard home

### MUST
- Ocean/sand/coral tokens in `src/styles.css` for both landing and shell
- Display = Instrument Serif; body = a loaded sans that is not Inter
- Empty week days have Assign / New schedule, not grey void
