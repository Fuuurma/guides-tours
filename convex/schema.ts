import { defineSchema } from "convex/server";

export default defineSchema({
	// Domain tables added in Phase 3 (schema port).
	// For Phase 2, Better Auth manages its own tables via the component.
});